{
  "name": "@icetype-examples/duckdb",
  "version": "0.1.0",
  "private": true,
  "description": "Generate DuckDB DDL from IceType schemas",
  "type": "module",
  "scripts": {
    "generate": "npx tsx generate-ddl.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@icetype/core": "workspace:*",
    "@icetype/duckdb": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
