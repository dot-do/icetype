{
  "name": "@icetype-examples/sqlite",
  "version": "0.1.0",
  "private": true,
  "description": "Generate SQLite DDL from IceType schemas",
  "type": "module",
  "scripts": {
    "generate": "npx tsx generate.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@icetype/core": "workspace:*",
    "@icetype/sqlite": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
